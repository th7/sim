# Bugs

Observed defects to work down during **Stabilize** (AGENTS.md → Work Loop). Reported from running the
game; the test suite is green, so these are gaps between the tests and real behaviour. Fixed bugs are
removed (their resolution lives in the commit + regression test).

_No open bugs._
