[package]
name = "protocol"
version = "0.1.0"
edition = "2021"
description = "Shared wire protocol + geometry for the sim server and the native client"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
